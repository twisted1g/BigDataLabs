---
title: "PISA"
output: html_document
---

```{r}
# install.packages("learningtower")
# library(learningtower)
# 
# students_data <- load_student("all")
# students_data
# 
# saveRDS(students_data, "students_data.rds")
```

```{r}
library(learningtower)
library(dplyr)
library(zoo)

setwd("~/Code/r/BigDataLabs/lab3")

```

\

## 1. Выбрать данные согласно варианту по указанным 7-ми странам, освободить от NA.

```{r}
students_data <- readRDS("./students_data.rds")
students_data
```

Подготовим данные для анализа.

Для стран: Китай, Индия нет определенного идентификатора. Для этих стран определены только области с уточнением принадлежности к стране. Зменим их на название страны.

```{r}
new_students_data <- left_join(students_data, countrycode, 
                               by = c("country" = "country"))

cat("Кол-во регионов до импутации:",length(unique(new_students_data$country_name)))

new_students_data$country_name[grepl("China",
                new_students_data$country_name) ] <- "China"

new_students_data$country_name[grepl("India",
                new_students_data$country_name) ] <- "India"

cat("\nКол-во регионов после импутации:", length(unique(new_students_data$country_name)))
```

```{r}
countries <- c(
  "Australia",
  "Japan",
  "China",
  "India",
  "Indonesia",
  "United States",
  "Turkey",
  "Greece"
)

students <- new_students_data %>%
  filter(country_name %in% countries)

cat("Страны в выборке:", paste(unique(students$country_name), collapse = ", "))
```

В даннных есть пропуски. Ниже предлагаются несколько способов по обработке пропусков.

Самое базовое - оставить na

Удаление na.

На графиках появляются разрывы.

```{r}
# students <- students %>%
#   filter(
#     !is.na(math),
#     !is.na(read),
#     !is.na(science),
#     !is.na(computer_n)
#   )
```

Импутация

Появляются выбросы. Можно убрать (это ниже), т.к. данные распередены нормально

```{r}
students$math[is.na(students$math)] <- 0
students$read[is.na(students$read)] <- 0
students$science[is.na(students$science)] <- 0
students$computer_n[is.na(students$computer_n)] <- 0
```

Линейная интерполяция

С одной стороны самый логичный способ. Но из-за этого нарушается распределение данных

```{r}
# library(zoo)
# 
# safe_approx <- function(x, year) {
#   if (sum(!is.na(x)) < 2) return(x)
#   tryCatch(
#     na.approx(x, x = year, rule = 2, na.rm = FALSE),
#     error = function(e) x  # при любой ошибке возвращаем оригинал
#   )
# }

# students <- students %>%
#   arrange(country_name, year) %>%
#   group_by(country_name) %>%
#   mutate(
#     math = safe_approx(math, year),
#     read = safe_approx(read, year),
#     science = safe_approx(science, year),
#   ) %>%
#   ungroup()
```

```{r, fig.width=12, fig.height=5}
par(mfrow = c(1,1))
for (subj in subjects <- c("math", "read", "science")){
  subj
  hist(students[[subj]],
       main = paste("Распределение оценок по", subj),
       xlab = "Оценка",
       col = "lightgreen")
}
par(mfrow = c(1,1))
```

Данные распределены нормально, однако присутствуют выбросы. Применим правило трёх сигм для их удаления.

```{r}
mean_math <- mean(students$math, na.rm = TRUE)
sd_math <- sd(students$math, na.rm = TRUE)

mean_read <- mean(students$read, na.rm = TRUE)
sd_read <- sd(students$read, na.rm = TRUE)

mean_science <- mean(students$science, na.rm = TRUE)
sd_science <- sd(students$science, na.rm = TRUE)

cat("Размер датасета до удаления выбросов:", nrow(students))

students <- students %>%
  filter(
    math >= mean_math - 3*sd_math & math <= mean_math + 3*sd_math,
    read >= mean_read - 3*sd_read & read <= mean_read + 3*sd_read,
    science >= mean_science - 3*sd_science & science <= mean_science + 3*sd_science
  )

cat("\nРазмер датасета после удаления выбросов:", nrow(students))
```

## 2. Подсчет средних оценок по трем признакам для каждого года.

```{r}
mean_scores <- students %>% 
  group_by(country_name, year) %>%
  summarise(
    mean_math = mean(math, na.rm = TRUE),
    mean_read = mean(read, na.rm = TRUE),
    mean_science = mean(science, na.rm = TRUE),
    .groups = "drop"
  )

mean_scores
```

## 3. Графики динамики изменения средних оценок и столбчатые диаграммы.

Для каждой страны построим графики изменения средних оценок по математике, чтению и науке по годам, а также столбчатую диаграмму за крайний год.

```{r, fig.width=9, fig.height=5}
for (country in countries) {
  country_data <- mean_scores %>%
    filter(country_name == country)

  y_min <- min(c(country_data$mean_math,
                 country_data$mean_read,
                 country_data$mean_science), na.rm = TRUE) * 0.95
  y_max <- max(c(country_data$mean_math,
                 country_data$mean_read,
                 country_data$mean_science), na.rm = TRUE) * 1.05

  plot(country_data$year, country_data$mean_math,
       type = "b", lty = 5, col = "blue",
       ylim = c(y_min, y_max),
       xlab = "Год", ylab = "Средние оценки",
       main = country, axes = FALSE)

  axis(1, at = country_data$year, las = 1)
  axis(2, at = pretty(c(y_min, y_max), n = 8), las = 1)

  lines(country_data$year, country_data$mean_read,
        col = "red", type = "b", lty = 5)
  lines(country_data$year, country_data$mean_science,
        col = "green", type = "b", lty = 5)

  legend("topright",
         legend = c("Математика", "Чтение", "Наука"),
         col = c("blue", "red", "green"),
         pch = 1, lty = 5)

  year_country_data <- country_data %>%
    filter(year == max(year))

  bp <- barplot(c(year_country_data$mean_math,
                  year_country_data$mean_read,
                  year_country_data$mean_science),
                names.arg = c("Математика", "Чтение", "Наука"),
                col = c("blue", "red", "green"),
                ylab = "Средние оценки",
                ylim = c(0, y_max),
                main = paste("Средние оценки в", country, "за",
                             max(year_country_data$year), "год"),
                width = 0.6, space = 0.8, axes = FALSE)

  axis(1, at = bp, labels = c("Математика", "Чтение", "Наука"))
  axis(2, at = pretty(c(0, y_max), n = 8), las = 1)
}
```

## 4. Круговая диаграмма средних оценок по полу.

Разделим датасет по гендерному признаку и построим круговую диаграмму средних оценок по трём признакам для мужчин и женщин (шесть долек).

```{r}
gender_counts <- table(students$gender)
percent <- round(gender_counts / sum(gender_counts) * 100, 1)

pie(gender_counts,
    labels = paste(names(gender_counts), percent, "%"),
    col = c("lightblue", "pink"),
    main = "Распределение студентов по полу")
    
```

## 5. Гистограммы распределений оценок по математике, чтению и науке для мужчин и женщин.

```{r, fig.width=12, fig.height=5}
par(mfrow = c(1,2))
for (subj in subjects <- c("math", "read", "science")){
  subj
  hist(students[[subj]][students$gender == "male"],
       main = paste("Распределение оценок по", subj, "(мужчины)"),
       xlab = "Оценка",
       col = "lightblue")
  
  hist(students[[subj]][students$gender == "female"],
       main = paste("Распределение оценок по", subj, "(женщины)"),
       xlab = "Оценка",
       col = "pink")
}
par(mfrow = c(1,1))
```

## 6. Сопоставление динамики изменения оценок по странам.

Построим в одной плоскости графики по всем странам для каждого предмета.

```{r, fig.width=10, fig.height=6}
colors <- rainbow(length(countries))

for (subj in c("math", "read", "science")) {
  plot(NULL,
       xlim = range(mean_scores$year),
       ylim = c(min(mean_scores$mean_math, mean_scores$mean_read, mean_scores$mean_science),
                max(mean_scores$mean_math, mean_scores$mean_read, mean_scores$mean_science) * 1.15),
       xlab = "Год",
       ylab = "Средние оценки",
       main = paste("Динамика оценок по", subj, "по странам"),
       xaxt = "n"
       )
  
  axis(1, at=mean_scores$year, las=1)
  
  for (i in seq_along(countries)) {
    country_data <- mean_scores %>% filter(country_name == countries[i])
    
    lines(country_data$year, country_data[[paste0("mean_", subj)]],
          type = "b",
          lty = 3,
          col = colors[i],
          pch = i)
  }
  
    legend("top",
         legend = countries,
         lty = 3,
         col = colors,
         pch = seq_along(countries),
         ncol = 2)
}
```

## 7. Среднее количество компьютеров у учащихся: первый, средний и последний год.

Подсчитаем среднее количество компьютеров для трёх временных точек по каждой стране и построим круговые диаграммы.

Для начала обработаем категориальный признак computer_n.

```{r}
unique(students$computer_n)

students$computer_n <- as.numeric(as.character(
  recode(as.character(students$computer_n), "3+" = "3")
))

unique(students$computer_n)
```

Среднее количество особо не меняется для разных способов обработки na

```{r}
# students <- students %>%
#   filter(!is.na(computer_n))
```

```{r, fig.width=7, fig.height=5}

for (country_full in countries) {
  country_data <- students %>%
    filter(country_name == country_full)
  
  years <- sort(unique(country_data$year))
  first_year  <- years[1]
  last_year   <- years[length(years)]
  middle_year <- years[round(length(years) / 2)]
  
  avg_computers <- country_data %>%
    filter(year %in% c(first_year, middle_year, last_year)) %>%
    group_by(year) %>%
    summarise(mean_computers = mean(computer_n, na.rm = TRUE))
  
  avg_computers <- avg_computers %>% filter(is.finite(mean_computers))
  
  if (nrow(avg_computers) == 0) next
  
  pie(avg_computers$mean_computers,
      labels = paste0(avg_computers$year, "\n", round(avg_computers$mean_computers, 2)),
      col = c("steelblue", "orange", "lightgreen")[1:nrow(avg_computers)],
      main = paste("Среднее количество компьютеров в", country_full))
}
```

## Дополнительный анализ

Рассмотрим кореляцию между уровнем достатка семьи и средним баллом студента.

Для этог посчитаем коэффициент корреляции Пирса.

```{r}
students_avg <- students %>%
  mutate(average_score = (math + read + science) / 3) %>%
  select(year, country_name, wealth, average_score)

students_avg <- students_avg %>% 
  filter(!is.na(wealth), !is.na(average_score))

cor(students_avg$average_score, students_avg$wealth)
cor.test(students_avg$average_score, students_avg$wealth)

```

Наглядно отобразим распределение значений и линию регрессии.

```{r}
plot(students_avg$wealth, students_avg$average_score,
     xlab = "Достаток",
     ylab = "Средний балл",
     main = "Корреляция",
     pch = 19,
     col = "blue",
     cex = 0.2)
     
abline(lm(average_score ~ wealth, data = students_avg), 
       col = "red", lwd = 2)
```
